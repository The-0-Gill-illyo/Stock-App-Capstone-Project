


export default function DisplayStockSearch(props){

    function handleClick(stock){

        console.log(stock)
        let stockId = stock.id.stockId
    }
    
    return(
       <h1>{props.searchStocks.ask}</h1>
    )
        }
